# decent.broker

The coming-soon page for [decent.broker](https://decent.broker).

This is a dependency-free static site. Open `index.html` directly or serve the
directory with any static web server.
